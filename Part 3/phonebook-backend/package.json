{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "Exercises",
  "main": "index.js",
  "engines": {
    "node": "14.12.0"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../fullstack2020/Part3/phonebook-frontend && npm run build --prod && cp -r build ../fullstack2020/Part3/phonebook-backend",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && npm run deploy",
    "logs:prod": "heroku logs --tail"
  },
  "proxy": "http://localhost:3001",
  "author": "Francisco Zavala",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "kill-port": "^1.6.1",
    "mongoose": "^5.10.11",
    "mongoose-unique-validator": "^2.0.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
